import cn from './cn';
import colors from './colors';
import generats from './generats';
import object from './object';
import validations from './validations';

export default { cn, object, validations, colors, generats };
